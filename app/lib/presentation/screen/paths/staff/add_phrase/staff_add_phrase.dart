import 'package:flutter/material.dart';

class StaffAddPhraseScreen extends StatelessWidget {
  const StaffAddPhraseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Container(
      color: Colors.pink.shade100,
      width: 200,
      height: 200,
      padding: const EdgeInsets.all(100),
      child: const Text("StaffAddPhraseScreen"),
    ));
  }
}
